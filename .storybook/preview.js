/** @type { import('@storybook/html-vite').Preview } */
const preview = {
  parameters: {
    controls: {
      disable: true,
    },
    actions: {
      disable: true,
    },
  },
};

export default preview;
